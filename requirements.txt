# Versiones fijas — probadas juntas, así "pip install -r requirements.txt" te
# da exactamente lo mismo que corre acá, no lo último que haya salido ese día.
# Para actualizar algo: cambiá el número a mano y volvé a correr los tests.
Django==6.0.6
python-dotenv==1.2.2
django-htmx==1.27.0
requests==2.34.2
django-axes==8.3.1

# Fotos de perfil: convertir HEIC/HEIF (formato por default de fotos de
# iPhone) a JPEG antes de guardar — sin esto, subir una foto directo del
# rollo de fotos de un iPhone fallaba en silencio.
Pillow==12.3.0
pillow-heif==1.5.0

# Deploy (Render) — servidor de producción, Postgres, static/media
gunicorn==26.0.0
whitenoise==6.12.0
dj-database-url==3.1.2
psycopg[binary]==3.2.13
django-storages==1.14.6
boto3==1.43.46
